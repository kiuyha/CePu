fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- database (fase Rate Limiting & Struktur DB) ---
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
asyncpg==0.29.0

# --- cache / rate limiting ---
redis==5.0.8

# --- validator paralel ---
dnspython==2.6.1

# --- seed script (scripts/seed_company_registry.py) ---
pandas==2.2.3
openpyxl==3.1.5

# --- Integrasi BERT (fase 7) ---
# CATATAN: ini dependency BESAR (~200MB-1GB tergantung platform), proses
# instalasi bisa 5-15 menit tergantung koneksi. Model sendiri (498MB)
# didownload TERPISAH secara otomatis oleh aplikasi saat pertama kali
# dijalankan (bukan lewat pip), lihat README bagian "Fase 7".
transformers==4.44.2
torch==2.4.1

# --- dev/test dependencies ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
fakeredis==2.24.1
